let setAndMap = "setAndMap";

{
    const s = new Set();
    let set = new Set(['red', 'green', 'blue']);
    // console.log(set)
    for (let item of set.entries()) {
        // console.log(item);
      }
// [2, 3, 5, 4, 5, 2, 2].forEach(x => s.add(x));
// console.log(s)


}

export {setAndMap};